var router = require('koa-router')();
var userManager = require('../system_modules/user');

// router.get('/getUser',userManager.findOne);

// router.get('/saveUser',userManager.saveUser);

module.exports = router;
